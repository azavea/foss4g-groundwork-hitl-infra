version: "3"

services:
  terraform:
    image: hashicorp/terraform:1.0.6
    volumes:
      - $HOME/.aws:/root/.aws
      - .:/opt/src
    entrypoint: 
      - sh
    working_dir: /opt/src
    environment:
      - AWS_PROFILE